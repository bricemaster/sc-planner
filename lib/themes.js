/**
 * @file Theme module — single theme, no switching needed.
 * Kept as stub so imports don't break.
 */
export function initThemeSwitcher() {}
export function applyTheme() {}
export function applyManufacturerTheme() {}
